import 'package:flutter/material.dart';

// Paddings
const EdgeInsets kCardPadding = EdgeInsets.all(16.0);

// Image
const double kImageHeight = 140.0;

// Radius
const double kCardRadius = 16.0;
const double kImageRadius = 12.0;

// Durations
const Duration kFadeDuration = Duration(milliseconds: 300);

